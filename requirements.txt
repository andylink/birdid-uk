# ── Detector dependencies ──────────────────────────────────────────────────────
birdnet-analyzer
sounddevice
soundfile
scipy
sqlalchemy
astral>=3.2
paho-mqtt
silero-vad          # neural VAD for privacy filter (clip-level human speech detection)

# ── Dashboard dependencies ─────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sse-starlette>=1.8.2
aiosqlite>=0.20.0
httpx>=0.27.0
librosa>=0.10.0
matplotlib>=3.8.0
numpy>=1.26.0
passlib[bcrypt]>=1.7.4   # password hashing for admin auth
itsdangerous>=2.1.2      # signed session cookie values for admin auth
slowapi>=0.1.9           # rate limiting for the admin login endpoint

# ── PostgreSQL backend (also installed for SQLite deployments; small overhead) ─
psycopg2-binary   # sync driver used by the detector
asyncpg           # async driver used by the dashboard

# ── Test dependencies ──────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ── Optional: Perch v2 inference backend ───────────────────────────────────────
# Set [inference] model = "perch" in config.toml to enable.
#
# 1. Install the package (pulls in TensorFlow CPU; ~2 GB):
#      pip install 'perch-hoplite[tf]'
#    For GPU support use:
#      pip install 'perch-hoplite[tf-cuda]'
#
# 2. Obtain Kaggle credentials so the model can be downloaded on first run
#    (~400 MB, cached in ~/.cache/kagglehub/ afterwards):
#      - Create an API token at https://www.kaggle.com/settings
#      - Save it to ~/.config/kaggle/kaggle.json
#        {"username":"<you>","key":"<token>"}
#      - Or export KAGGLE_USERNAME and KAGGLE_KEY as environment variables.
#
# perch-hoplite[tf]
