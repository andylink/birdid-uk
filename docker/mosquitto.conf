# Mosquitto MQTT broker — minimal config for Docker Compose
# Allows anonymous connections from the birdid container on port 1883.
# For production use, enable password_file and TLS.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
